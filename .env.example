# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# LLM Configuration
# Provider: openai, claude, deepseek
LLM_PROVIDER=openai
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL_NAME=gpt-4o-mini

# Notion (optional)
NOTION_ENABLED=false
NOTION_API_KEY=your_notion_api_key_here
NOTION_DATABASE_ID=your_notion_database_id_here

# Context Settings
MAX_CONTEXT_TURNS=10
CONTEXT_EXPIRE_MINUTES=30

# Admin / Manual Takeover
# Admin chat ID (personal or group) for receiving manual-mode notifications
ADMIN_CHAT_ID=0
# Comma-separated list of admin user IDs authorized for /takeover, /release, /reply
ADMIN_USER_IDS=[]
# Confidence threshold (0.0-1.0) — below this, auto-escalate to manual
CONFIDENCE_THRESHOLD=0.6

# Accounting backends (comma-separated: sqlite,notion,excel)
ACCOUNTING_BACKENDS=sqlite

# Logging
LOG_LEVEL=INFO
